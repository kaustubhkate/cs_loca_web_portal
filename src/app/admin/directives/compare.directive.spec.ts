import { CompairDirective } from './compare.directive';

describe('CompareDirective', () => {
  it('should create an instance', () => {
    const directive = new CompairDirective();
    expect(directive).toBeTruthy();
  });
});
