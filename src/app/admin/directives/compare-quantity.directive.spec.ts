import { CompareQuantityDirective } from './compare-quantity.directive';

describe('CompareQuantityDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareQuantityDirective();
    expect(directive).toBeTruthy();
  });
});
